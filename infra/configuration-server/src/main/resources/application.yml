server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fwfurtado/gash-configurations
      enabled: false

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false